name: hello workflow
on:
  push:
    branches: [main]
jobs:
  greet:
    runs-on: ubuntu-latest
      steps: 
        - name: say hello to users
          run: echo "Hello DPchamoli"
          run: echo "Hello $USER"

          
    
